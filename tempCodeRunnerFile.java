import java.util.*;
class tempCodeRunnerFile{
    //     public static ArrayList<Integer> findTriplet(int arr[], int n)
    // {
    //     // sort the array
    //     Arrays.sort(arr);
 
    //     // for every element in arr
    //     // check if a pair exist(in array) whose
    //     // sum is equal to arr element
    //     for (int i = n - 1; i >= 0; i--) {
    //         int j = 0;
    //         int k = i - 1;
    //         while (j < k) {
    //             if (arr[i] == arr[j] + arr[k]) {
 
    //                 // triplet found
    //                 return new ArrayList<Integer>(Arrays.asList(arr[i], arr[j], arr[k]));
    //             }
    //             else if (arr[i] > arr[j] + arr[k])
    //                 j += 1;
    //             else
    //                 k -= 1;
    //         }
    //     }
    //     return new ArrayList<Integer>();
    // }
    public static void main(String[] args) {
        int[] arr = {1,2,3,4,5} ;
        // System.out.println(findTriplet(arr, arr.length));
        System.out.println("shyam" > "ram");
    }

}